spring:
  datasource:
    url: jdbc:mysql://158.180.66.65:12001/ai
    username: mysql
    password: gsm1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  task:
    scheduling:
      pool:
        size: 5

cloud:
  aws:
    s3:
      bucket: ${ BUCKET }
    credentials:
      access-key: ${ ACCESS-KEY }
      secret-key: ${ SECRET-KEY }
    region:
      static: ${ REGION }
      auto: false
    stack:
      auto: false